<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright (C) 2014 The Android Open Source Project
    modified
    SPDX-License-Identifier: Apache-2.0 AND GPL-3.0-only
-->

<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/config_suggestions_strip_height">
    <!-- To ensure that key preview popup is correctly placed when the current system locale is
         one of RTL locales, layoutDirection="ltr" is needed in the SDK version 17+. -->
    <helium314.keyboard.latin.suggestions.SuggestionStripView
        android:id="@+id/suggestion_strip_view"
        android:layoutDirection="ltr"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        style="?attr/suggestionStripViewStyle" />
    <LinearLayout
        android:id="@+id/emoji_tab_strip"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="?attr/suggestionStripViewStyle" />
    <HorizontalScrollView
        android:id="@+id/clipboard_strip_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="?attr/suggestionStripViewStyle"
        android:scrollbarThumbHorizontal="@color/toolbar_scrollbar" >
        <LinearLayout
            android:id="@+id/clipboard_strip"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="end" >
            <!-- empty view to move buttons to the right -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />
        </LinearLayout>
    </HorizontalScrollView>
    <HorizontalScrollView
        android:id="@+id/prompt_strip_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="?attr/suggestionStripViewStyle"
        android:scrollbarThumbHorizontal="@color/toolbar_scrollbar"
        android:visibility="gone">
        <LinearLayout
            android:id="@+id/prompt_strip"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="end">
            <!-- empty view to move buttons to the right -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />
        </LinearLayout>
    </HorizontalScrollView>
    <HorizontalScrollView
        android:id="@+id/desktop_shortcuts_strip_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        style="?attr/suggestionStripViewStyle"
        android:scrollbarThumbHorizontal="@color/toolbar_scrollbar"
        android:visibility="gone">
        <LinearLayout
            android:id="@+id/desktop_shortcuts_strip"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="end">
            <!-- empty view to move buttons to the right -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />
        </LinearLayout>
    </HorizontalScrollView>
    <LinearLayout
        android:id="@+id/voice_preview_strip"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="6dp"
        style="?attr/suggestionStripViewStyle"
        android:visibility="gone">
        <TextView
            android:id="@+id/voice_preview_text"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:maxLines="2"
            android:ellipsize="end"
            android:textSize="12.5sp"
            android:includeFontPadding="false"
            android:lineSpacingMultiplier="0.95"
            android:gravity="center_vertical|start" />
        <ImageView
            android:id="@+id/voice_preview_clear"
            android:layout_width="44dp"
            android:layout_height="match_parent"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close_rounded"
            android:contentDescription="@string/voice_preview_clear" />
    </LinearLayout>
</FrameLayout>
